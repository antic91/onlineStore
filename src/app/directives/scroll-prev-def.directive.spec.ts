import { ScrollPrevDefDirective } from './scroll-prev-def.directive';

describe('ScrollPrevDefDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollPrevDefDirective();
    expect(directive).toBeTruthy();
  });
});
