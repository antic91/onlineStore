import { RoundingNumbersPipe } from './rounding-numbers.pipe';

describe('RoundingNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundingNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
